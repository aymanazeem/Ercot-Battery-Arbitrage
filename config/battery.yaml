battery:
  power_mw: 1.0
  duration_h: 2.0
  round_trip_efficiency: 0.85
  initial_soc_frac: 0.5
  cycles_per_day_cap: null          # null means no cap on cycles per day
cost:
  cycling_cost_per_mwh: 0.0         # the headline run uses zero, sensitivity runs override
  pack_cost_per_kwh: 200.0
  cycle_life_at_80pct_dod: 1000
